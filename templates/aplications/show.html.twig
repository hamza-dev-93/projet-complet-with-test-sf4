{% extends 'base.html.twig' %}

{% block title %}Aplications{% endblock %}

{% block body %}
    <h1>Aplications</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aplication.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ aplication.name }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ aplication.createdAt ? aplication.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('aplications_index') }}">back to list</a>

    <a href="{{ path('aplications_edit', {'id': aplication.id}) }}">edit</a>

    {{ include('aplications/_delete_form.html.twig') }}
{% endblock %}
